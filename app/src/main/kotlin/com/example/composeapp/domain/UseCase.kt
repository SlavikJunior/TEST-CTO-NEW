package com.example.composeapp.domain

/**
 * Слой логики бизнеса (Domain Layer)
 * Содержит use cases - классы, реализующие бизнес-логику приложения
 * Независим от фреймворков, UI и деталей реализации
 * Работает с сущностями предметной области и правилами бизнеса
 */
interface UseCase {
    // UseCase interface placeholder
}
